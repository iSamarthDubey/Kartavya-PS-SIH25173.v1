# 🚀 SYNRGY DEMO - Render Deployment Requirements
# Optimized for Kartavya SIH 2025 Hackathon Demo
# Cloud-safe, fast builds, minimal footprint

# ===== CORE WEB FRAMEWORK =====
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# ===== SYSTEM & UTILITIES =====
psutil>=5.9.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
python-json-logger>=2.0.0

# ===== DATA PROCESSING (Essential Only) =====
pandas>=2.1.0
numpy>=1.24.0
requests>=2.31.0
aiohttp>=3.9.0

# ===== NLP STACK (Lightweight) =====
nltk>=3.8.1
textstat>=0.7.3
dateparser>=1.2.0
# spacy>=3.7.0  # Comment out for faster builds

# ===== SIEM & SEARCH =====
elasticsearch>=8.11.0
faiss-cpu>=1.7.0  # Only if vector search demo needed

# ===== DEMO AI (Hackathon Only) =====
google-generativeai>=0.3.0

# ===== SECURITY =====
cryptography>=41.0.0
bcrypt>=4.1.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ===== DATABASES (Cloud Demo) =====
asyncpg>=0.29.0
redis>=5.0.0
supabase>=2.0.0

# ===== DEMO DATA =====
datasets>=2.16.0

# ===== REMOVED FROM DEMO (Too Heavy for Render) =====
# ❌ torch - 2GB+ download, breaks free tier builds
# ❌ transformers - 500MB+, not needed for rule-based NLP  
# ❌ sentence-transformers - Heavy, using basic embeddings
# ❌ streamlit - Not used in FastAPI backend
# ❌ matplotlib/plotly/seaborn - Charts generated client-side
# ❌ scikit-learn - Using rule-based NLP primarily
# ❌ motor/pymongo - Using PostgreSQL for demo
# ❌ openai - Using Gemini only for demo fallback

# 🎯 OPTIMIZED FOR:
# - Render free tier (512MB RAM limit)
# - Build time under 10 minutes
# - Essential features only
# - SIH demo requirements
