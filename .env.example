# Environment configuration for local development (PowerShell syntax on Windows)
# Copy this file to .env (same directory) and adjust values as needed.

# Assistant API / UI
SIEM_BACKEND_PORT=8000
SIEM_FRONTEND_PORT=5173

# SIEM connector
ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200
# ELASTICSEARCH_USERNAME=
# ELASTICSEARCH_PASSWORD=
# ELASTICSEARCH_INDEX=security-logs

# ============================================================================= 
# DATA SOURCE CONFIGURATION
# =============================================================================

# ENVIRONMENT: demo (uses dataset fallback) | production (real sources only)
ENVIRONMENT=demo

# Data Source Operation Mode
# single: Use only one best data source
# multi/auto: Use ALL available REAL data sources (production recommended)
DATA_SOURCE_MODE=auto

# Primary Data Source Selection
# auto: Auto-detect all available sources (recommended)
# elasticsearch: Elasticsearch/ELK Stack
# wazuh: Wazuh SIEM
# splunk: Splunk Enterprise
DEFAULT_DATA_SOURCE=auto

# Multi-Source Configuration (for REAL sources)
ENABLE_MULTI_SOURCE=true
MAX_CONCURRENT_SOURCES=3
MULTI_SOURCE_TIMEOUT=30.0

# Correlation fields for deduplication across sources
CORRELATION_FIELDS=source.ip,destination.ip,user.name,@timestamp

# =============================================================================
# PRODUCTION vs DEMO BEHAVIOR:
# =============================================================================
# 🏢 PRODUCTION MODE: ZERO TOLERANCE - REAL DATA ONLY
#    ✅ ONLY Elasticsearch/Wazuh/Splunk allowed
#    ❌ NO dataset/demo data (disabled for security)
#    ❌ NO fallbacks (system fails if no real sources)
#    🔥 FAIL-FAST: Startup blocked if no real sources available
#    🎯 Purpose: Mission-critical ISRO production deployment
# 
# 🎭 DEMO MODE: FLEXIBLE - PERFECT FOR HACKATHONS
#    ✅ Real sources preferred (Elasticsearch/Wazuh/Splunk)
#    ✅ Dataset fallback allowed (50k security records)
#    ✅ Always works (no external dependencies required)
#    🚀 Purpose: Demonstrations, development, hackathons

# Legacy support (use DEFAULT_DATA_SOURCE instead)
# SIEM_PLATFORM=elasticsearch
# Optional hint for field mappings selection (windows|linux|network)
INDEX_CLASS=

# Security
# REQUIRED on first launch to bootstrap admin securely (no defaults created)
ASSISTANT_ADMIN_PASSWORD=
# Rate limits (requests per window)
ASSISTANT_QUERY_RATE=60
ASSISTANT_QUERY_WINDOW=60
ASSISTANT_LOGIN_RATE=10
ASSISTANT_LOGIN_WINDOW=300
